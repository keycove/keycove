from setuptools import setup, find_packages

setup(
    name="keycove",
    version="0.1.0",
    description="A package for api key authorization.",
    packages=find_packages(),
    install_requires=[],
)
